<!-- ~/.pi/agent-style prompt: plan-then-approve (report §10.6) -->

Plan first, do not touch any files yet.

1. Research the current state (read-only) relevant to this goal:
   {{focus}}
2. Write the plan to `.harness/MISSION.md` using the mission-control format:
   goal line, constraints, and an ordered checklist of concrete verifiable
   steps. For each step that mutates state, note the risk (reversible /
   irreversible).
3. Present me a digest: the goal, the number of steps, any irreversible
   steps flagged in red terms, and what you are unsure about.
4. Stop and wait for my explicit approval. Do not implement until I approve.

After approval: work the checklist top-down, ticking steps as you complete
them (recitation protocol), and let the verification gate run the checks.
